len(arr)                   # longitud de la lista
sum(arr)                   # suma de los elementos
max(arr)                   # mayor elemento
min(arr)                   # menor elemento
sorted(arr)                # nueva lista ordenada (ascendente)
sorted(arr, reverse=True)  # orden descendente
list(reversed(arr))        # reversa como lista nueva
